import React, { Fragment } from 'react';
import addIcon from './addIcon.png';

const AddButton = () => 
    <Fragment>
        <img src={addIcon} alt="+" style={{  margin: 'auto', display: 'block' }} />
    </Fragment>
        

export default AddButton;
